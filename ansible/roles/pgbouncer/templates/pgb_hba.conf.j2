# PgBouncer HBA configuration
local   all     all                                     trust
host    all     all     127.0.0.1/32            trust
host    all     all     ::1/128                  trust
{% for network in pgbouncer_trusted_networks | default(['10.0.0.0/8']) %}
host    all     all     {{ network }}            md5
{% endfor %}